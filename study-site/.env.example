# Paystack — from your Paystack dashboard (Settings > API Keys & Webhooks)
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_live_xxxxxxxx
PAYSTACK_SECRET_KEY=sk_live_xxxxxxxx

# Any long random string — used to sign timed download links.
# Generate one with: openssl rand -hex 32
TOKEN_SECRET=replace-with-a-long-random-string
